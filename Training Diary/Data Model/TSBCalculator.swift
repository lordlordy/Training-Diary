//
//  TSBCalculator.swift
//  Training Diary
//
//  Created by Steven Lord on 18/10/2017.
//  Copyright © 2017 Steven Lord. All rights reserved.
//

import Foundation
/* DEPRECATED
class TSBCalculator{
    
    private var ctl: Double = 0.0
    private var atl: Double = 0.0
    private let atlDays = 7.0 //should be got from TrainingDiary
    private let ctlDays = 42.0 //should be got from TrainingDiary

    func calculate(forTSS t: Double) -> (atl: Double, ctl: Double, tsb: Double){
        ctl = t * (1 - exp(-1/ctlDays)) + ctl * exp(-1/ctlDays)
        atl = t * (1 - exp(-1/atlDays)) + atl * exp(-1/atlDays)
        return (atl, ctl, ctl-atl)
    }
}
*/
